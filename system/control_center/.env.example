# MQTT Broker (PC1 -- localhost if broker is on the same machine)
MQTT_BROKER_HOST=127.0.0.1
MQTT_BROKER_PORT=1883

# Flask web server
FLASK_HOST=0.0.0.0
FLASK_PORT=8080

# Vehicle IDs to monitor (comma-separated)
VEHICLE_IDS=vehicle_0
